node_modules
dist
.env.local
.env
*.local
.DS_Store
*.tsbuildinfo
# Herramientas locales de Claude Code (launch.json de la vista previa, etc.)
.claude/
# Fotos de prueba sueltas en la RAÍZ (con / para no tapar los iconos
# de la PWA en public/, que sí se versionan)
/*.jpg
/*.jpeg
/*.png
# El manual para repartir (7+ MB con capturas incrustadas): se genera
# desde Claude Code, no se versiona
/Manual de Incidencias.html
# Restos de pruebas locales de generación de documentos (Erik, 22-sep-2026)
/outputs/
/build_biobox_pipeline_doc.py
supabase/.temp/
# Cuentas de la prueba de carga CON sus contraseñas: las escribe
# scripts/usuarios-carga.mjs y las lee k6. Nunca al repo (auditoría primer
# mes, 24-sep-2026).
tests/carga/.usuarios.json
